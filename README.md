<div align="center">

# buylow

**한국 주식(KOSPI·KOSDAQ) 자동매매 전략을 코드 없이 구성하고 과거 데이터로 백테스트하는 개인용 툴킷.**

[QuantConnect LEAN](https://github.com/QuantConnect/Lean) 엔진 위에서 동작하며, 동일한 전략 정의가 백테스트와 (추후) 실거래에서 그대로 실행됩니다.

<sub>현재 문서는 한국어로 제공됩니다. English / 日本語 번역은 추후 추가 예정입니다.</sub>

</div>

---

> **상태: 개발 중.** 전략 작성 · 데이터 적재 · 백테스트(일봉 + 장중 분봉 타이밍) · 리스크 관리까지 동작합니다.
> 증권사 연동은 한국투자증권(KIS)부터 — 인증·시세(일봉/분봉) 데이터 계층이 동작하며, 대시보드에서 증권사를 고릅니다.
> 실거래(실주문) 실행 엔진은 KIS용으로 구현돼 있습니다(LEAN 라이브 + C# `IBrokerage` 어댑터, `adapter/MyTrading.Kis`).
> 다만 안전상 **무장(arming) 스위치 + 주문금액 한도** 뒤에 잠겨 있어, 켜기 전에는 절대 주문을 내지 않습니다.
> 실전(real) 사용 전 **모의투자(demo)** 로 먼저 검증하십시오([docs/LIVE_KIS.md](./docs/LIVE_KIS.md)).
> 토스증권은 API 공개 대기 중(동일 어댑터 형태로 추가 예정). 현재 권장 용도는 백테스트·분석입니다.

## 개요

- 내 PC에서만 동작하는 로컬 웹 대시보드를 제공합니다. 브라우저로 `http://127.0.0.1:8420`에 접속하며, 시세 데이터와 API 키는 모두 로컬에만 저장됩니다(외부로 전송되지 않음).
- 전략은 **지표(시그널) 조합 규칙 + 리스크 설정**으로 정의합니다. `(EMA AND MACD) OR RSI`와 같은 매수 규칙을 체크박스로 구성하고, 손절·익절 등 리스크를 함께 지정합니다.
- 7가지 시그널을 제공합니다: 추세(EMA·MACD·모멘텀), 과열(RSI·볼린저밴드), 가치(저PER·저PBR), 한국 시장 특화 수급 추종(외국인·기관 순매수).
- 한국시장 전 종목 데이터로 백테스트를 실행하고, 결과를 한국어 요약과 거래 내역(매매 시점·종목·사유)으로 확인합니다.
- 공매도 없이(롱온리) 손절·익절·트레일링으로 포지션을 관리합니다.

## 아키텍처

```
브라우저 대시보드 (localhost:8420)
        │
Python 오케스트레이터 (상시 실행, FastAPI)
        │  작업마다 LEAN 프로세스를 생성
        ▼
LEAN 엔진 (.NET) — 전략을 백테스트(과거 재생) / (추후) 라이브로 실행
```

전략을 한 번 작성하면 백테스트와 실거래에 동일하게 사용됩니다(LEAN 동형성). 모든 처리는 사용자의 PC에서 이루어집니다.

## 기능

### 1. 시그널(알파)

각 시그널은 매 거래일 종목별로 매수 우호 / 매도 우호 / 중립을 판단합니다. 전략 설정 탭에서 파라미터를 조정합니다.

| 시그널 | 유형 | 설명 | 주요 파라미터 |
|---|---|---|---|
| EMA | 추세 | 단기 이동평균이 장기 이동평균 위면 매수 우호, 아래면 매도 우호 | 단기·장기 기간 |
| MACD | 추세·모멘텀 | MACD선이 시그널선 위면 매수 우호 | 단기·장기·시그널 |
| RSI | 과열·역추세 | 과매도면 매수 우호, 과매수면 매도 우호 | 기간·과매도·과매수 |
| 모멘텀 | 추세 | 최근 N일 수익률이 양수면 매수 우호 | 관측 기간 |
| 볼린저밴드 | 평균회귀·돌파 | 밴드 터치 시 평균회귀, 강하게 돌파하면 추세 추종으로 전환 | 기간·표준편차 배수·전환 임계(%) |
| 저평가(가치) | 펀더멘털 | 저PER·저PBR이면서 ROE(=PBR/PER)가 기준 이상이면 매수 우호(가치 함정 회피) | PER·PBR 상한·ROE·배당 하한 |
| 수급 추종 | 한국 시장 특화 | 외국인·기관·개인(선택) 최근 N일 누적 순매수가 양수면 매수 우호 | 누적 일수·투자자 선택 |

가치·수급 시그널은 펀더멘털·수급 데이터가 적재되어 있어야 동작합니다(아래 *데이터 관리* 참고).

### 2. 매수 규칙

지표를 불리언 규칙으로 조합합니다. 대시보드의 조건 그룹 빌더에서 체크박스로 구성합니다.

- 한 그룹 안의 조건은 모두 충족(AND), 그룹끼리는 하나만 충족(OR)하면 매수합니다.
- 예: `[EMA + MACD]` 그룹과 `[RSI]` 그룹 → (EMA 그리고 MACD) 또는 (RSI).
- 신호 보유 기간: 매수 신호가 마지막으로 발생한 날로부터 며칠 더 보유할지 지정합니다. 매도 신호가 나오면 즉시 청산하고, 신호가 사라지면 이 기간 후 청산합니다.

전략은 하나만 저장되며(단일 전략), 변경 후 상단 저장 버튼으로 보관합니다.

### 3. 리스크 관리

매수는 전략이, 매도는 신호 변화와 아래 리스크 두 축으로 결정됩니다. 항목을 비우면 미적용입니다.

- 종목 손절(%): 매수가 대비 N% 하락 시 매도.
- 종목 익절(%): 평가이익 N% 도달 시 매도.
- 트레일링 스탑(%): 매수 후 고점 대비 N% 하락 시 매도(고점 자동 추적으로 이익 보호).
- 롱온리(공매도 차단)와 동시 보유 종목 상한(자본 대비 종목이 과다하면 유동성 상위만 보유)이 기본 적용됩니다.

### 4. 체결 타이밍 (장중 진입/청산)

전략은 두 층으로 동작합니다.
**① 선별** — 신호로 그날 매수/청산 대상을 고릅니다.
**② 타이밍** — 그 대상을 언제 체결할지 정합니다.
전략 설정의 "리스크 · 체결" 카드에서 **해상도**를 고르면 나머지가 자동으로 정해집니다.

- 해상도 **일봉**:
  - 선별·리스크 평가는 전날 종가 기준 하루 1회로 고정됩니다.
  - 체결 시점만 고릅니다 — **다음 거래일 시가** 또는 **다음 거래일 종가**(MarketOnClose).
  - 신호는 당일 종가에 계산되므로, 룩어헤드 방지를 위해 체결은 항상 다음 거래일입니다.
- 해상도 **분봉**:
  - 선별·리스크 평가는 장중 매분으로 고정됩니다.
  - 체결은 **TWAP(시간 분할)** 로 고정되고, **분할 수**만 지정합니다 — 정규장(390분)을 N등분해 수량을 1/N씩 나눠 체결(시장충격 완화). 미체결 잔량은 장 마감 전 강제 체결(옵션).
  - 분봉이 없는 종목/날짜는 일봉(시가)으로 자동 폴백합니다.
- 같은 타이밍 코드가 백테스트와 (추후) 라이브에서 동일하게 동작합니다. 분봉 백테스트에는 분봉 데이터(KIS 적재)가 필요합니다.

### 5. 백테스트

- 기간 선택(날짜 선택기 및 1주·1달·3달·6달·1년 전 빠른 버튼).
- 유니버스 구성: 종목명·코드 검색 추가, 인덱스 일괄추가(KOSPI200·KOSDAQ150), 전체 종목 대상.
- 초기자본은 1억원으로 고정됩니다.
- 백그라운드로 실행되며 진행률과 로그를 작업중 탭에서 확인합니다. 실행 이력은 보관됩니다(SQLite).
- 결과는 한국어 요약(총 수익률·최종 자산·최대 낙폭·승률·샤프 지수 등)과 거래 내역(날짜·종목명·매수/매도·수량·금액·트리거 사유)으로 제공됩니다.

### 6. 데이터 관리 · 증권사 연동

- 데이터 최신화 한 번으로 한국시장 전 종목의 가격(OHLCV)·수급(투자자별 순매수)·펀더멘털(PER/PBR)을 적재합니다.
  - 데이터 종류별로 마지막 적재일 다음날부터 오늘까지 증분 적재하며, 비어 있는 종류는 최근 5년을 백필합니다.
  - 일봉 가격은 무인증(pykrx)으로 받으며, 수급·펀더멘털은 KRX 로그인이 필요합니다.
- **자동 적재 스케줄러**(기본 켜짐): 서버가 켜져 있는 동안 **기본 30분마다** 위 일봉 데이터를 자동으로 증분 적재합니다(이미 채워졌으면 금방 끝남). 데이터 탭에서 분봉 대상종목을 골라 **'자동적재 대상으로 저장'**하면 분봉도 같은 주기로 함께 받습니다(분봉은 증권사 API라 대상종목 지정 필요). 간격·on/off는 `config.local.yaml`의 `scheduler.interval_minutes`/`scheduler.enabled`로 조정합니다.
- 종목명 자동 매핑(코드↔이름), 적재 현황 검색(이름·코드) 및 **인덱스 필터**(전체종목·KOSPI200·KOSDAQ150 등으로 목록 거르기), 종목 상세 조회(가격·수급 전체 기간과 날짜 필터)를 제공합니다.
- **내 인덱스(커스텀 종목 그룹)**: **그룹 탭**에서 원하는 종목들을 묶어 이름을 붙여두면, **백테스트·분봉적재·적재현황에서 KOSPI200처럼 ★이름으로** 한 번에 골라 쓸 수 있습니다(생성·수정·삭제 — 저장된 그룹의 [수정]으로 종목/이름 편집).
- **증권사 선택**: 설정 탭에서 증권사를 고릅니다 — **KIS 실전 / KIS 모의투자 / 토스증권(미구현)**. KIS는 실전과 모의투자의 앱키·계좌가 완전히 분리돼 있어 **각각 따로** 키를 입력해 관리합니다(같은 로직, 환경만 다름). 매매(잔고·주문)는 고른 증권사 서버(모의면 모의 서버)를 쓰고, **시세·분봉 적재는 모의투자를 골라도 항상 실전 서버**에서 받습니다(계좌가 필요 없는 조회라 더 안정적).
- **누적 분봉 적재**: 데이터 탭에서 종목을 직접 입력하거나 인덱스(KOSPI200·KOSDAQ150)를 골라 분봉을 받아 디스크에 저장합니다(기간 1달~1년 선택). 이미 받은 날짜는 건너뜁니다. 거래일들을 병렬로 받되 KIS 유량 한도 안에서 호출률을 제한하므로(공유 토큰버킷) 직렬 적재보다 빠르고 안전합니다. 백테스트는 **분봉이 있는 날은 장중 타점 전략**을, **없는 종목/일은 시가 기준**으로 진행합니다. KIS는 과거 분봉을 **최대 약 1년**만 보관하므로 그 이전 구간은 분봉 백테스트가 불가합니다(일봉 백테스트는 전체 기간 가능).
- **분봉 시드 데이터(빠른 시작)**: 전종목 분봉을 KIS로 처음부터 받으면 오래 걸리므로, 레포가 GitHub 릴리스로 분봉 묶음(시드)을 제공합니다. `scripts/fetch_minute_seed.sh`로 받아 `data/`에 풀면 바로 분봉 백테스트가 가능하고, 부족한 종목·기간은 위의 **분봉 최신화**로 증분만 채웁니다(이미 있는 날은 건너뜀). 데이터는 레포 히스토리에 쌓지 않고 릴리스 에셋으로 두어 클론은 가볍게 유지합니다.
  ```bash
  bash scripts/fetch_minute_seed.sh   # gh/토큰 불필요 — 공개 릴리스에서 curl로 받음
  ```

### 대시보드 구성

전략 설정(기본 화면) · 백테스트 · 데이터 · 그룹(내 인덱스) · 설정(API 키) · 작업중(백그라운드 진행) · **매매**(라이브).

**매매 탭**은 증권사 계좌 모니터링·제어 화면입니다: 계좌 정보, 예수금·보유종목(매수가/현재가/손익),
장중/장마감 상태, 매매 내역(날짜별), **대상종목(인덱스·그룹·검색으로 선택)**, 그리고 **자동매매 on/off**.
자동매매를 켜면 저장된 전략+대상종목으로 LEAN 라이브가 돌고(분봉=1분봉마다), 끄면 종료됩니다.
잔고와 매매 내역은 **10초마다 자동 갱신**됩니다.
보유종목·매매내역은 **KIS 계좌의 실제 데이터**라, KIS 앱/HTS로 직접 한 주문·보유도 그대로 보입니다(매매 내역은
체결조회 `inquire-daily-ccld`). 체결조회를 지원하지 않는 증권사(토스 등)는 buylow 자체 주문기록으로 대체됩니다.
실전 자동매매는 무장(arming) 후에만 켜지고, 먼저 모의투자(demo)로 검증하십시오([docs/LIVE_KIS.md](./docs/LIVE_KIS.md)).

## 설치

다음 소프트웨어가 필요합니다.

| 소프트웨어 | 용도 |
|---|---|
| [.NET 10 SDK](https://dotnet.microsoft.com/download) | LEAN 엔진 실행 |
| [Python 3.11](https://www.python.org/) | LEAN 전략(pythonnet)은 3.11이 필요합니다 |
| [uv](https://github.com/astral-sh/uv) | 파이썬 환경·패키지 관리 |
| git | 코드 내려받기 |
| (선택) [KRX 계정](https://data.krx.co.kr) | 수급·펀더멘털 데이터용(무료 가입). 가격만 사용할 경우 불필요 |

### macOS

`.NET 10 SDK`를 설치하고 환경 변수를 설정합니다(영구 적용은 `~/.zshrc`에 추가).

```bash
curl -fsSL https://dot.net/v1/dotnet-install.sh | bash -s -- --channel 10.0 --install-dir "$HOME/.dotnet"
export DOTNET_ROOT="$HOME/.dotnet" && export PATH="$HOME/.dotnet:$PATH"
```

Python 3.11, git, uv를 설치합니다.

```bash
brew install python@3.11 git
curl -LsSf https://astral.sh/uv/install.sh | sh
```

코드를 내려받고 의존성을 설치합니다.

```bash
git clone https://github.com/JeongSeongMok/buylow.git
cd buylow
uv venv .venv && uv pip install --python .venv/bin/python -e ".[dev]"
```

(선택) 분봉 백테스트를 빠르게 시작하려면 분봉 시드 데이터를 받습니다. 생략해도 되며, 분봉은 나중에 대시보드 데이터 탭에서 직접 적재할 수 있습니다(부족분은 증분으로만 받습니다).

```bash
bash scripts/fetch_minute_seed.sh   # gh/토큰 불필요 — 공개 릴리스에서 curl로 받음
```

대시보드를 실행한 뒤 브라우저에서 `http://127.0.0.1:8420`에 접속합니다.

```bash
.venv/bin/python -m orchestrator.api
```

### Windows (PowerShell)

.NET 10 SDK, Python 3.11, git, uv를 설치합니다. 설치 후 새 터미널을 열어 PATH를 반영합니다.

```powershell
winget install Microsoft.DotNet.SDK.10
winget install Python.Python.3.11
winget install Git.Git
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
```

코드를 내려받고 의존성을 설치합니다.

```powershell
git clone https://github.com/JeongSeongMok/buylow.git
cd buylow
uv venv .venv
uv pip install --python .venv\Scripts\python.exe -e ".[dev]"
```

(선택) 분봉 백테스트를 빠르게 시작하려면 분봉 시드 데이터를 받습니다. 생략 가능하며, 분봉은 나중에 대시보드 데이터 탭에서 직접 적재할 수 있습니다. 시드 스크립트는 bash이므로 Git Bash에서 실행합니다.

```bash
bash scripts/fetch_minute_seed.sh   # Git Bash에서 실행 — 공개 릴리스에서 curl로 받음
```

대시보드를 실행한 뒤 브라우저에서 `http://127.0.0.1:8420`에 접속합니다.

```powershell
.venv\Scripts\python -m orchestrator.api
```

### KRX 키 설정 (수급·펀더멘털 사용 시)

대시보드 설정 탭에서 KRX 아이디·비밀번호를 입력하거나, `config.example.yaml`을 `config.local.yaml`로 복사해 채웁니다. `config.local.yaml`은 저장소에 커밋되지 않습니다.

```yaml
# config.local.yaml
data_folder: ./data
dashboard_port: 8420
secrets:
  krx_id: ""     # https://data.krx.co.kr 무료 가입
  krx_pw: ""
```

## 사용 시작

브라우저로 `http://127.0.0.1:8420`에 접속한 뒤:

1. 데이터 탭에서 데이터 최신화를 실행합니다. 최초 1회는 필수이며(백테스트에 과거 데이터가 필요), 전 종목 적재이므로 작업중 탭에서 진행 상황을 확인합니다.
2. (선택) 분봉 백테스트를 빠르게 시작하려면 `bash scripts/fetch_minute_seed.sh`로 분봉 시드를 받습니다. 부족분은 데이터 탭의 분봉 최신화로 증분 채웁니다.
3. 전략 설정 탭에서 지표 값을 조정하고, 매수 규칙(조건 그룹)을 구성하고, 리스크를 설정한 뒤 저장합니다.
4. 백테스트 탭에서 기간과 종목을 선택하고 실행한 뒤, 결과와 거래 내역을 확인합니다. 결과 화면 하단의 **신호 진단(추정)**에서 매수신호 발생률과 어느 신호가 매수를 막았는지(예: 수급 FLOW) 경향을 볼 수 있습니다.

## 파이프라인

```
데이터 최신화(가격·수급·펀더멘털) → ./data
        │
        ▼  (LEAN 5단계 · 매 거래일)
유니버스 → 시그널 판단(매수/매도) → 포트폴리오(롱온리·비중) → 리스크(손절·익절·트레일링) → 주문
        │
        ▼
백테스트(현재) = 과거 재생   /   라이브(예정) = 토스 실시간·주문
```

매수는 전략(시그널 규칙)이, 매도는 신호 변화와 리스크가 결정하는 두 축 구조입니다.

## 면책 조항

이 소프트웨어는 교육 목적으로 제공됩니다. 자동 매매는 상당한 금융 리스크를 수반하며, 사용에 따른 책임은 전적으로 사용자에게 있습니다. 제작자는 어떠한 금전적 손실에도 책임지지 않습니다. 사용 시 증권사 API 약관 및 관련 법규를 반드시 준수하십시오. 또한 백테스트 결과는 과거 데이터 기반 추정이며 미래 수익을 보장하지 않습니다.

## 라이선스

미정(TBD).
